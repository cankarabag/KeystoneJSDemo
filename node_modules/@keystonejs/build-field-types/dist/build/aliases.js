"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getAliases = getAliases;

var _path = _interopRequireDefault(require("path"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getAliases(project, getKey = x => x) {
  let aliases = {};
  project.packages.forEach(pkg => {
    pkg.entrypoints.map(x => x.strict()).forEach(entrypoint => {
      aliases[getKey(entrypoint.name)] = _path.default.join(pkg.name, _path.default.relative(entrypoint.directory, entrypoint.source));
    });
  });
  return aliases;
}