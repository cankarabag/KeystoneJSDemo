import { format } from 'date-fns';

const CalendarDayCell = _ref => {
  let {
    data,
    field: {
      config
    }
  } = _ref;

  if (!data) {
    return null;
  }

  if (!config.format) {
    return data;
  }

  return format(data, config.format);
};

export default CalendarDayCell;
