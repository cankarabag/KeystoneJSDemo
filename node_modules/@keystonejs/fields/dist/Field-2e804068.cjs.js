'use strict';

var core = require('@emotion/core');

var fields = require('@arch-ui/fields');

var dayPicker = require('@arch-ui/day-picker');
/** @jsx jsx */


const DateTimeField = _ref => {
  let {
    autoFocus,
    field,
    onChange,
    value,
    errors
  } = _ref;
  const htmlID = "ks-input-".concat(field.path);
  return core.jsx(fields.FieldContainer, null, core.jsx(fields.FieldLabel, {
    htmlFor: htmlID,
    field: field,
    errors: errors
  }), field.config.adminDoc && core.jsx(fields.FieldDescription, null, field.config.adminDoc), core.jsx(fields.FieldInput, null, core.jsx(dayPicker.TextDayTimePicker, {
    id: htmlID,
    date: value,
    onChange: onChange,
    autoFocus: autoFocus
  })));
};

exports.default = DateTimeField;
