'use strict';

var dateFns = require('date-fns');

const CalendarDayCell = _ref => {
  let {
    data,
    field: {
      config
    }
  } = _ref;

  if (!data) {
    return null;
  }

  if (!config.format) {
    return data;
  }

  return dateFns.format(data, config.format);
};

exports.default = CalendarDayCell;
