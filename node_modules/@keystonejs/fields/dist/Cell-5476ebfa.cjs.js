'use strict';

var core = require('@emotion/core');
/** @jsx jsx */


const Image = _ref => {
  let {
    alt,
    src
  } = _ref;
  return core.jsx("div", {
    css: {
      alignItems: 'center',
      display: 'flex',
      height: 24,
      lineHeight: 0,
      width: 24
    }
  }, core.jsx("img", {
    alt: alt,
    css: {
      maxHeight: '100%',
      maxWidth: '100%'
    },
    src: src
  }));
};

var Cell = _ref2 => {
  let {
    data
  } = _ref2;
  if (!data) return null;
  const isImage = data.mimetype.includes('image');
  const truncatedFilename = data.filename.replace("".concat(data.id, "-"), '');
  return isImage ? core.jsx(Image, {
    alt: truncatedFilename,
    src: data.publicUrl
  }) : core.jsx("div", null, truncatedFilename);
};

exports.default = Cell;
