import React__default, { Fragment } from 'react';

var Cell = _ref => {
  let {
    data,
    field,
    Link
  } = _ref;

  if (!data) {
    return null;
  }

  const refList = field.adminMeta.getListByKey(field.config.ref);
  return React__default.createElement(Fragment, null, (Array.isArray(data) ? data : [data]).filter(item => item).map((item, index) => React__default.createElement(Fragment, {
    key: item.id
  }, !!index ? ', ' : '', React__default.createElement(Link, {
    path: refList.path,
    item: item
  }, item._label_))));
};

export default Cell;
