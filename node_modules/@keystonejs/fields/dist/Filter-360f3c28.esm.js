import _defineProperty from '@babel/runtime/helpers/esm/defineProperty';
import React__default, { Component } from 'react';
import { RadioGroup, Radio } from '@arch-ui/filters';

class PasswordFilterView extends Component {
  constructor() {
    super(...arguments);

    _defineProperty(this, "handleChange", value => {
      const boolValue = value === 'is_set' ? true : false;
      this.props.onChange(boolValue);
    });
  }

  render() {
    const {
      value
    } = this.props;
    const textValue = value ? 'is_set' : 'is_not_set';
    return React__default.createElement(RadioGroup, {
      onChange: this.handleChange,
      value: textValue
    }, React__default.createElement(Radio, {
      value: "is_set"
    }, "Is Set"), React__default.createElement(Radio, {
      value: "is_not_set"
    }, "Is NOT Set"));
  }

}

export default PasswordFilterView;
