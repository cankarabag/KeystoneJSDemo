'use strict';

function _interopDefault(ex) {
  return ex && typeof ex === 'object' && 'default' in ex ? ex['default'] : ex;
}

var React = require('react');

var React__default = _interopDefault(React);

var Cell = _ref => {
  let {
    data,
    field,
    Link
  } = _ref;

  if (!data) {
    return null;
  }

  const refList = field.adminMeta.getListByKey(field.config.ref);
  return React__default.createElement(React.Fragment, null, (Array.isArray(data) ? data : [data]).filter(item => item).map((item, index) => React__default.createElement(React.Fragment, {
    key: item.id
  }, !!index ? ', ' : '', React__default.createElement(Link, {
    path: refList.path,
    item: item
  }, item._label_))));
};

exports.default = Cell;
