import _defineProperty from '@babel/runtime/helpers/esm/defineProperty';
import { format } from 'date-fns';
import { TextDayPicker } from '@arch-ui/day-picker';
import React__default, { Component } from 'react';
const FORMAT = 'YYYY-MM-DD';

class CalendarDayFilterView extends Component {
  constructor(props) {
    super(props);

    _defineProperty(this, "handleSelectedChange", value => {
      const {
        onChange
      } = this.props;

      if (value === null) {
        value = format(new Date(), FORMAT);
      }

      onChange(value);
      this.setState({
        value
      });
    });

    this.state = {
      value: format(new Date(), FORMAT)
    };
  }

  render() {
    const {
      filter
    } = this.props;
    if (!filter) return null;
    return React__default.createElement(TextDayPicker, {
      date: this.state.value,
      onChange: this.handleSelectedChange
    });
  }

}

export default CalendarDayFilterView;
