import { jsx } from '@emotion/core';
import { FieldContainer, FieldLabel, FieldDescription, FieldInput } from '@arch-ui/fields';
import { TextDayTimePicker } from '@arch-ui/day-picker';
/** @jsx jsx */

const DateTimeField = _ref => {
  let {
    autoFocus,
    field,
    onChange,
    value,
    errors
  } = _ref;
  const htmlID = "ks-input-".concat(field.path);
  return jsx(FieldContainer, null, jsx(FieldLabel, {
    htmlFor: htmlID,
    field: field,
    errors: errors
  }), field.config.adminDoc && jsx(FieldDescription, null, field.config.adminDoc), jsx(FieldInput, null, jsx(TextDayTimePicker, {
    id: htmlID,
    date: value,
    onChange: onChange,
    autoFocus: autoFocus
  })));
};

export default DateTimeField;
