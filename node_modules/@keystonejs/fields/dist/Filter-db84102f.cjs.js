'use strict';

function _interopDefault(ex) {
  return ex && typeof ex === 'object' && 'default' in ex ? ex['default'] : ex;
}

var _defineProperty = _interopDefault(require("@babel/runtime/helpers/defineProperty"));

var React = require('react');

var React__default = _interopDefault(React);

var filters = require('@arch-ui/filters');

class PasswordFilterView extends React.Component {
  constructor() {
    super(...arguments);

    _defineProperty(this, "handleChange", value => {
      const boolValue = value === 'is_set' ? true : false;
      this.props.onChange(boolValue);
    });
  }

  render() {
    const {
      value
    } = this.props;
    const textValue = value ? 'is_set' : 'is_not_set';
    return React__default.createElement(filters.RadioGroup, {
      onChange: this.handleChange,
      value: textValue
    }, React__default.createElement(filters.Radio, {
      value: "is_set"
    }, "Is Set"), React__default.createElement(filters.Radio, {
      value: "is_not_set"
    }, "Is NOT Set"));
  }

}

exports.default = PasswordFilterView;
