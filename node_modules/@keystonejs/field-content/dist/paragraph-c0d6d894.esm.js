import { jsx } from '@emotion/core';
import _extends from '@babel/runtime/helpers/esm/extends';
let type = 'paragraph';

function Node(_ref) {
  let {
    attributes,
    children
  } = _ref;
  return jsx("p", _extends({}, attributes, {
    css: {
      ':first-of-type': {
        marginTop: 0
      },
      ':last-of-type': {
        marginBottom: 0
      }
    }
  }), children);
}

export { Node, type };
