import { jsx } from '@emotion/core';
import _extends from '@babel/runtime/helpers/esm/extends';
let type = 'caption';

function Node(_ref) {
  let {
    attributes,
    children
  } = _ref;
  return jsx("figcaption", _extends({
    css: {
      padding: 8,
      textAlign: 'center'
    }
  }, attributes), children);
}

export { Node, type };
