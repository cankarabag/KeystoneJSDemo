'use strict';

function _interopDefault(ex) {
  return ex && typeof ex === 'object' && 'default' in ex ? ex['default'] : ex;
}

var core = require('@emotion/core');

var _extends = _interopDefault(require("@babel/runtime/helpers/extends"));

let type = 'caption';

function Node(_ref) {
  let {
    attributes,
    children
  } = _ref;
  return core.jsx("figcaption", _extends({
    css: {
      padding: 8,
      textAlign: 'center'
    }
  }, attributes), children);
}

exports.Node = Node;
exports.type = type;
