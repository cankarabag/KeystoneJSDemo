'use strict';

function _interopDefault(ex) {
  return ex && typeof ex === 'object' && 'default' in ex ? ex['default'] : ex;
}

var core = require('@emotion/core');

var _extends = _interopDefault(require("@babel/runtime/helpers/extends"));

let type = 'paragraph';

function Node(_ref) {
  let {
    attributes,
    children
  } = _ref;
  return core.jsx("p", _extends({}, attributes, {
    css: {
      ':first-of-type': {
        marginTop: 0
      },
      ':last-of-type': {
        marginBottom: 0
      }
    }
  }), children);
}

exports.Node = Node;
exports.type = type;
